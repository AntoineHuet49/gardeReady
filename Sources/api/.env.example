# Variables d'environnement pour l'API

# Port d'écoute (Railway l'attribue automatiquement)
PORT=3000

# Base de données PostgreSQL (Railway fournira ces valeurs)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=gardeready
DB_USER=root
DB_PASSWORD=root
DATABASE_URL=postgresql://root:root@localhost:5432/gardeready

# JWT Secret (à générer aléatoirement pour la production)
JWT_SECRET=your-super-secret-jwt-key-here

# Configuration CORS (URL du frontend)
FRONTEND_URL=http://localhost:5173

# Configuration Mailjet (si utilisé)
MAILJET_API_KEY=your-mailjet-api-key
MAILJET_SECRET_KEY=your-mailjet-secret-key

# Environnement
NODE_ENV=production