name: CI/CD O2switch FTP
on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy to O2switch
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22'
        cache: 'npm'
        cache-dependency-path: |
          Sources/api/package-lock.json
          Sources/client/package-lock.json

    - name: Install dependencies API
      run: |
        cd Sources/api
        npm ci
      continue-on-error: false

    - name: Build API
      run: |
        cd Sources/api
        echo "🔨 Building API..."
        npx tsc
        echo "✅ API build terminé"
        ls -la dist/
      continue-on-error: false

    - name: Install dependencies Client
      run: |
        cd Sources/client
        npm ci
      continue-on-error: false

    - name: Create production environment files
      run: |
        # Créer .env pour le client AVANT le build
        cd Sources/client
        cat > .env << EOF
        NODE_ENV=production
        VITE_API_URL=${{ secrets.API_URL }}
        EOF
        
        # Créer .env pour l'API
        cd ../api
        cat > .env << EOF
        NODE_ENV=production
        PORT=8080
        DATABASE_URL=${{ secrets.DATABASE_URL }}
        JWT_SECRET=${{ secrets.JWT_SECRET }}
        FRONTEND_URL=${{ secrets.FRONTEND_URL }}
        MAILJET_API_KEY=${{ secrets.MAILJET_API_KEY }}
        MAILJET_SECRET_KEY=${{ secrets.MAILJET_SECRET_KEY }}
        EOF

    - name: Build Client
      run: |
        cd Sources/client
        echo "🔨 Building Client..."
        echo "📋 Variables d'environnement disponibles:"
        cat .env
        echo "🚀 Démarrage du build..."
        npm run build
        echo "✅ Client build terminé"
        ls -la dist/
        # Copier .htaccess dans le build si il existe
        cp .htaccess dist/ 2>/dev/null || echo "⚠️ .htaccess non trouvé, sera créé plus tard"
      continue-on-error: false

    - name: Prepare Frontend deployment
      run: |
        # Créer un dossier de déploiement pour le frontend
        mkdir -p deploy-frontend
        
        # Vérifier que le build du client existe
        if [ ! -d "Sources/client/dist" ]; then
          echo "❌ Sources/client/dist non trouvé"
          exit 1
        fi
        
        echo "📊 Frontend - Fichiers à déployer: $(ls -1 Sources/client/dist/ | wc -l) fichiers"
        
        # Copier le client
        cp -r Sources/client/dist/* deploy-frontend/
        
        # Créer un .htaccess pour React Router (SPA)
        cat > deploy-frontend/.htaccess << 'EOF'
        # Configuration pour React Router (SPA)
        RewriteEngine On
        RewriteRule ^index\.html$ - [L]
        
        # Ne pas rediriger les requêtes vers l'API
        RewriteCond %{REQUEST_URI} !^/api/
        RewriteCond %{REQUEST_FILENAME} !-f
        RewriteCond %{REQUEST_FILENAME} !-d
        RewriteRule . /index.html [L]
        EOF
        
        echo "✅ Frontend prêt pour déploiement"

    - name: Prepare API deployment
      run: |
        # Créer un dossier de déploiement pour l'API
        mkdir -p deploy-api
        
        # Vérifier que le build de l'API existe
        if [ ! -d "Sources/api/dist" ]; then
          echo "❌ Sources/api/dist non trouvé"
          exit 1
        fi
        
        echo "📊 API - Fichiers à déployer: $(ls -1 Sources/api/dist/ | wc -l) fichiers"
        echo "📋 Structure du build API:"
        find Sources/api/dist -type f -name "*.js" | head -10
        
        # Copier TOUT le contenu compilé (pas seulement app.js)
        cp -r Sources/api/dist/* deploy-api/
        cp Sources/api/package.json deploy-api/
        cp Sources/api/.env deploy-api/
        
        # Créer un .htaccess pour l'API (redirection vers Node.js)
        cat > deploy-api/.htaccess << 'EOF'
        # Configuration pour API Node.js sur O2switch
        RewriteEngine on
        
        # Rediriger toutes les requêtes vers l'application Node.js
        RewriteCond %{REQUEST_FILENAME} !-f
        RewriteCond %{REQUEST_FILENAME} !-d
        RewriteRule ^(.*)$ http://localhost:8080/$1 [P,L]
        
        # Headers pour API
        Header always set Access-Control-Allow-Origin "*"
        Header always set Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS"
        Header always set Access-Control-Allow-Headers "Content-Type, Authorization"
        EOF
        
        echo "✅ API prête pour déploiement (app.js + package.json + .env + .htaccess)"

    - name: Test FTP connection
      run: |
        echo "🔍 Test de connexion FTP..."
        timeout 10 curl -v ftp://${{ secrets.FTP_SERVER }}:21 --user "${{ secrets.FTP_USERNAME }}:${{ secrets.FTP_PASSWORD }}" || echo "❌ Connexion FTP échouée"

    - name: Clean problematic files via FTP
      run: |
        echo "🧹 Nettoyage des fichiers problématiques..."
        # Supprimer l'ancien dossier api dans public_html et le fichier .env
        curl -s --connect-timeout 10 --max-time 30 \
          ftp://${{ secrets.FTP_SERVER }}/public_html/ \
          --user "${{ secrets.FTP_USERNAME }}:${{ secrets.FTP_PASSWORD }}" \
          -Q "DELE .env" \
          -Q "RMD api" \
          || echo "⚠️ Nettoyage terminé (certains fichiers n'existaient peut-être pas)"

    - name: Deploy Frontend to public_html (with retry)
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      continue-on-error: true
      id: frontend-deploy
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        local-dir: './deploy-frontend/'
        server-dir: '/public_html/'
        port: 21
        timeout: 60000
        log-level: verbose
        security: loose
        dangerous-clean-slate: false
        dry-run: false
        exclude: |
          **/.git*
          **/.git*/**
          **/node_modules/**
          **/.DS_Store
          **/Thumbs.db
          **/*.env
          
    - name: Retry Frontend deployment if failed
      if: steps.frontend-deploy.outcome == 'failure'
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        local-dir: './deploy-frontend/'
        server-dir: '/public_html/'
        port: 21
        timeout: 120000
        log-level: minimal
        security: loose
        dangerous-clean-slate: false
        dry-run: false

    - name: Deploy API to api folder (with retry)
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      continue-on-error: true
      id: api-deploy
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        local-dir: './deploy-api/'
        server-dir: '/api/'
        port: 21
        timeout: 60000
        log-level: verbose
        security: loose
        dangerous-clean-slate: false
        dry-run: false
        exclude: |
          **/.git*
          **/.git*/**
          **/node_modules/**
          **/.DS_Store
          **/Thumbs.db
          
    - name: Retry API deployment if failed
      if: steps.api-deploy.outcome == 'failure'
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        local-dir: './deploy-api/'
        server-dir: '/api/'
        port: 21
        timeout: 120000
        log-level: minimal
        security: loose
        dangerous-clean-slate: false
        dry-run: false

    - name: Deployment complete
      run: |
        echo "✅ Déploiement terminé sur O2switch!"
        echo "🌐 Frontend déployé dans: /public_html/ (accessible via https://verifeu.fr)"
        echo "🔧 API déployée dans: /api/ (accessible via https://verifeu.fr/api)"
        echo "🚀 Déploiement en 2 étapes terminé avec succès!"
        echo "📋 Actions post-déploiement :"
        echo "1. Panel O2switch → Applications Node.js → Redémarrer"
        echo "2. Tester : https://verifeu.fr et https://verifeu.fr/api"